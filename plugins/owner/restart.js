export const run = {
   usage: ['إعادة_تشغيل'], // الاسم العربي الرئيسي الذي سيظهر في القائمة (إعادة_تشغيل)
   hidden: ['restart', 'ريستارت', 'تحديث_البوت'], // الاختصارات المخفية لضمان عمل البوت إذا كُتبت بالإنجليزية أو العربية البديلة
   category: 'المطور', // الفئة المعربة لتنظيم قائمة أوامر المالك
   async: async (m, {
      client,
      system,
      Utils
   }) => {
      // إرسال تنبيه البدء في إعادة التشغيل، ثم حفظ قاعدة البيانات وإعادة الإقلاع
      await client.reply(m.chat, Utils.texted('bold', 'جاري إعادة تشغيل النظام وحفظ البيانات . . . \n\n© DEV ABOODI OFFICIAL'), m).then(async () => {
         await system.database.save(global.db) // حفظ آمن للبيانات قبل الإغلاق
         process.send('reset') // إرسال أمر إعادة التشغيل للمشغل الرئيسي
      })
   },
   owner: true // الأداة محمية ومخصصة لمالك ومطور البوت الرئيسي فقط
}
